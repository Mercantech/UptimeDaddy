version: "3.9"

services:
  http_requester:
    build: ./http_requester
    image: http_requester
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - .env
    environment:
      HOST: host.docker.internal